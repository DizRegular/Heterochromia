package UniverseEngine;
import RenderObject.Camera;
import javax.swing.JFrame;

public class NormalRenderer extends Renderer {
    public NormalRenderer(JFrame window, Camera camera) {
        super(window, camera);
    }
    
}
